Ini adalah permintaan yang sangat komprehensif dan akademis. Saya akan menjawab kedua bagian secara lengkap dan terstruktur.

BAGIAN 1: PEMODELAN DATA DUMMY — MATA KULIAH SISTEM OPERASI
1.1 Kriteria Teoritis: Karakteristik Data Dummy yang Baik
Sebelum membuat data, ada beberapa prinsip penting yang perlu diperhatikan:
A. Menghindari Cold-Start Problem pada BKT
BKT membutuhkan setidaknya beberapa observasi per mahasiswa per materi untuk menghasilkan estimasi P(Ln) yang stabil. Rekomendasi minimal:

≥ 3 soal per submateri per mahasiswa agar update posterior BKT tidak terlalu volatile.
Inisialisasi P(L₀) tidak boleh 0.0 atau 1.0 — gunakan prior yang moderat (0.1–0.4) agar ada ruang untuk update.

B. Menghindari Bias pada K-Means

Class imbalance harus realistis: distribusi kemampuan mahasiswa sebaiknya mengikuti kurva normal (kebanyakan "cukup", sebagian kecil "mahir" dan "remedial").
Variasi antar materi harus ada: seorang mahasiswa bisa mahir di "Manajemen Proses" tapi lemah di "Virtual Memory" — ini yang membentuk feature vector yang meaningful untuk clustering.
Hindari data yang terlalu seragam: jika semua mahasiswa memiliki skor yang identik, K-Means akan gagal membentuk cluster yang bermakna (semua centroid akan collapse).

C. Representativitas Populasi (~40 Mahasiswa)
Distribusi yang disarankan untuk 40 mahasiswa:

Mahir (Cluster A): ~10 mahasiswa — skor tinggi di hampir semua materi (>75% benar)
Cukup (Cluster B): ~20 mahasiswa — performa merata tapi tidak dominan (40–75% benar)
Remedial (Cluster C): ~10 mahasiswa — skor rendah, terutama di materi lanjut (<40% benar)


1.2 Skema & Data CSV
File 1: mahasiswa.csv
csvNIM,Nama
2301001,Andi Prasetyo
2301002,Budi Santoso
2301003,Citra Lestari
2301004,Dian Rahayu
2301005,Eko Wahyudi
2301006,Farah Nadia
2301007,Gilang Ramadan
2301008,Hana Pertiwi
2301009,Irfan Maulana
2301010,Joko Susilo
2301011,Kiki Amalia
2301012,Lukman Hakim
2301013,Maya Sari
2301014,Nanda Kurnia
2301015,Oscar Firmansyah
2301016,Putri Wulandari
2301017,Qori Handayani
2301018,Rafi Hidayat
2301019,Sari Dewi
2301020,Taufik Hidayat
2301021,Ulfa Nurjanah
2301022,Vino Pratama
2301023,Wulan Sari
2301024,Xander Maulana
2301025,Yuni Astuti
2301026,Zaki Ramadhan
2301027,Arini Safitri
2301028,Bagus Permana
2301029,Clara Indah
2301030,Doni Setiawan
2301031,Elvira Putri
2301032,Farhan Akbar
2301033,Gita Puspita
2301034,Hendra Gunawan
2301035,Ika Sulistyawati
2301036,Jihan Fitriani
2301037,Kevin Mandala
2301038,Lina Marliana
2301039,Miko Saputra
2301040,Nina Agustina

File 2: kuis_struktur.csv
Struktur: 6 Materi Utama SISOP × 5 Submateri × 3 Soal = 90 soal total
csvID_Soal,Materi,Submateri,Bobot,Opsi_A,Opsi_B,Opsi_C,Opsi_D,Kunci
Q001,Manajemen Proses,Konsep Proses,1,"Program yang sedang dieksekusi","File yang tersimpan di disk","Instruksi dalam memori ROM","Unit penyimpanan data","A"
Q002,Manajemen Proses,Konsep Proses,1,"Ready → Running → Blocked","New → Ready → Running → Waiting → Terminated","Running → Ready → New","Blocked → Running → Terminated","B"
Q003,Manajemen Proses,Konsep Proses,1,"Process Control Block (PCB)","Memory Allocation Table","File Descriptor Table","Interrupt Vector Table","A"
Q004,Manajemen Proses,Thread dan Konkurensi,2,"Thread berbagi address space; Process tidak","Process berbagi address space; Thread tidak","Keduanya berbagi address space","Keduanya tidak berbagi address space","A"
Q005,Manajemen Proses,Thread dan Konkurensi,2,"Race condition","Deadlock","Starvation","Thrashing","A"
Q006,Manajemen Proses,Thread dan Konkurensi,2,"Mutex","Semaphore","Monitor","Spinlock","B"
Q007,Manajemen Proses,Sinkronisasi,2,"Mutual Exclusion","Progress","Bounded Waiting","Semua benar","D"
Q008,Manajemen Proses,Sinkronisasi,2,"wait() dan signal()","lock() dan unlock()","P() dan V() saja","acquire() dan release()","C"
Q009,Manajemen Proses,Sinkronisasi,2,"Deadlock","Starvation","Livelock","Race Condition","A"
Q010,Manajemen Proses,Deadlock,3,"Mutual Exclusion, Hold and Wait, No Preemption, Circular Wait","Mutual Exclusion, Preemption, Hold and Wait","No Preemption, Circular Wait saja","Hold and Wait, Starvation","A"
Q011,Manajemen Proses,Deadlock,3,"Resource Allocation Graph","Banker's Algorithm","Ostrich Algorithm","Detection Algorithm","B"
Q012,Manajemen Proses,Deadlock,3,"Preemption","Rollback","Kill process","Semua benar","D"
Q013,Penjadwalan CPU,Konsep Penjadwalan,1,"Memilih proses dari ready queue untuk dieksekusi CPU","Mengalokasikan memori untuk proses","Mengelola I/O request","Mengatur komunikasi antar proses","A"
Q014,Penjadwalan CPU,Konsep Penjadwalan,1,"Short-term scheduler","Long-term scheduler","Medium-term scheduler","I/O scheduler","A"
Q015,Penjadwalan CPU,Konsep Penjadwalan,1,"Throughput, Turnaround Time, Waiting Time, Response Time","Hanya Throughput dan Waiting Time","CPU Utilization saja","Turnaround Time dan Burst Time","A"
Q016,Penjadwalan CPU,Algoritma FCFS dan SJF,2,"First Come First Served","Shortest Job First","Round Robin","Priority Scheduling","A"
Q017,Penjadwalan CPU,Algoritma FCFS dan SJF,2,"Convoy Effect","Starvation","Aging","Thrashing","A"
Q018,Penjadwalan CPU,Algoritma FCFS dan SJF,2,"Starvation pada proses panjang","Tidak ada preemption","Membutuhkan prediksi burst time","Semua benar","D"
Q019,Penjadwalan CPU,Round Robin dan Priority,2,"Time Quantum","Burst Time","Priority Number","Response Time","A"
Q020,Penjadwalan CPU,Round Robin dan Priority,2,"Meningkatkan prioritas proses yang menunggu lama","Menurunkan prioritas semua proses","Merestart proses yang starvation","Menggunakan FCFS sebagai fallback","A"
Q021,Penjadwalan CPU,Round Robin dan Priority,2,"Preemptive Priority Scheduling","Non-Preemptive SJF","FCFS","Multilevel Queue","A"
Q022,Penjadwalan CPU,Multilevel Queue,3,"Proses permanen di satu queue","Proses dapat berpindah antar queue","Hanya ada satu queue","Queue hanya untuk I/O bound","A"
Q023,Penjadwalan CPU,Multilevel Queue,3,"Multilevel Feedback Queue","Round Robin","SJF","FCFS","A"
Q024,Penjadwalan CPU,Multilevel Queue,3,"Waktu dari submit hingga selesai eksekusi","Waktu menunggu di ready queue saja","Waktu eksekusi di CPU","Waktu I/O","A"
Q025,Manajemen Memori,Konsep Memori,1,"Logical Address","Physical Address","Virtual Address","Segmented Address","A"
Q026,Manajemen Memori,Konsep Memori,1,"Memory Management Unit (MMU)","Cache Controller","DMA Controller","Interrupt Controller","A"
Q027,Manajemen Memori,Konsep Memori,1,"Internal Fragmentation","External Fragmentation","Thrashing","Swapping","A"
Q028,Manajemen Memori,Paging,2,"Membagi memori menjadi frame berukuran tetap","Membagi memori secara logis","Teknik kompresi memori","Metode alokasi stack","A"
Q029,Manajemen Memori,Paging,2,"Page Table","Segment Table","Frame Table","Directory Table","A"
Q030,Manajemen Memori,Paging,2,"Translation Lookaside Buffer (TLB)","Page Table Cache","Segment Cache","Frame Buffer","A"
Q031,Manajemen Memori,Segmentasi,2,"Membagi memori berdasarkan unit logis (segment)","Membagi memori berdasarkan ukuran tetap","Menghapus fragmentasi eksternal","Teknik virtual memory","A"
Q032,Manajemen Memori,Segmentasi,2,"External Fragmentation","Internal Fragmentation","Thrashing","Page Fault","A"
Q033,Manajemen Memori,Segmentasi,2,"Segment number + Offset","Frame number + Offset","Page number + Offset","Block number + Offset","A"
Q034,Virtual Memory,Konsep Virtual Memory,1,"Menggunakan disk sebagai ekstensi RAM","Memori fisik yang sangat cepat","Cache antara CPU dan RAM","Memori GPU","A"
Q035,Virtual Memory,Konsep Virtual Memory,1,"Demand Paging","Prepaging","Swapping","Compaction","A"
Q036,Virtual Memory,Konsep Virtual Memory,1,"Page Fault","Cache Miss","TLB Miss","Segment Fault","A"
Q037,Virtual Memory,Page Replacement,2,"FIFO, LRU, Optimal, Clock","FIFO dan LRU saja","Optimal dan Clock saja","Hanya LRU","A"
Q038,Virtual Memory,Page Replacement,2,"Optimal (OPT)","LRU","FIFO","Clock","A"
Q039,Virtual Memory,Page Replacement,2,"Belady's Anomaly","Page Fault Paradox","LRU Anomaly","Frame Overflow","A"
Q040,Virtual Memory,Thrashing,3,"CPU sibuk swap page terus-menerus, utilitas CPU rendah","CPU idle karena tidak ada proses","Memori penuh tanpa page fault","Proses menunggu I/O terlalu lama","A"
Q041,Virtual Memory,Thrashing,3,"Working Set Model","Page Fault Frequency","Keduanya benar","Load Control saja","C"
Q042,Virtual Memory,Thrashing,3,"Mengurangi jumlah proses (degree of multiprogramming)","Menambah frame untuk semua proses","Menggunakan FIFO replacement","Meningkatkan time quantum","A"
Q043,File System,Konsep File System,1,"Cara OS mengorganisasi dan menyimpan data di storage","Teknik kompresi data","Metode enkripsi file","Protokol transfer file","A"
Q044,File System,Konsep File System,1,"Inode","File Control Block","FAT Entry","Directory Entry","A"
Q045,File System,Konsep File System,1,"Hierarchical (Tree)","Flat","Graph","Linear","A"
Q046,File System,Implementasi File System,2,"Contiguous, Linked, Indexed","Sequential, Random, Direct","Block, Byte, Record","Flat, Tree, Graph","A"
Q047,File System,Implementasi File System,2,"External Fragmentation","Internal Fragmentation","Page Fault","TLB Miss","A"
Q048,File System,Implementasi File System,2,"Indexed Allocation","Contiguous Allocation","Linked Allocation","FAT Allocation","A"
Q049,File System,Journaling dan Reliability,3,"Mencatat operasi sebelum eksekusi untuk pemulihan dari crash","Teknik backup data","Metode enkripsi transaksi","Kompresi log file","A"
Q050,File System,Journaling dan Reliability,3,"ext4, NTFS, XFS","FAT32, exFAT","Keduanya benar","Hanya ext4","C"
Q051,File System,Journaling dan Reliability,3,"Menulis metadata journal, Commit, Checkpoint","Backup, Restore, Verify","Write, Read, Delete","Mount, Unmount, Format","A"
Q052,I/O Management,Konsep I/O,1,"Mengelola komunikasi antara CPU dan perangkat I/O","Mengatur alokasi memori untuk I/O","Teknik penjadwalan disk saja","Manajemen file pada storage","A"
Q053,I/O Management,Konsep I/O,1,"Polling, Interrupt-Driven, DMA","Synchronous I/O saja","Asynchronous I/O saja","Buffered I/O saja","A"
Q054,I/O Management,Konsep I/O,1,"Device Driver","I/O Scheduler","DMA Controller","Interrupt Handler","A"
Q055,I/O Management,Disk Scheduling,2,"FCFS, SSTF, SCAN, C-SCAN, LOOK","FCFS dan SSTF saja","SCAN dan C-SCAN saja","Hanya SSTF","A"
Q056,I/O Management,Disk Scheduling,2,"SSTF (Shortest Seek Time First)","FCFS","SCAN","C-LOOK","A"
Q057,I/O Management,Disk Scheduling,2,"Starvation pada request yang jauh","Tidak ada starvation","Overhead tinggi","Seek time tinggi","A"
Q058,I/O Management,DMA dan Buffering,3,"CPU tidak perlu terlibat langsung dalam transfer data","CPU mengelola setiap byte transfer","DMA memperlambat transfer","DMA hanya untuk disk","A"
Q059,I/O Management,DMA dan Buffering,3,"Menyimpan data sementara saat transfer antara dua komponen","Teknik kompresi data I/O","Cache permanen untuk I/O","Register I/O di CPU","A"
Q060,I/O Management,DMA dan Buffering,3,"Single Buffer, Double Buffer, Circular Buffer","Hanya Single Buffer","Hanya Circular Buffer","Flat Buffer dan Tree Buffer","A"
(Catatan: Untuk dataset produksi, tambahkan soal Q061–Q090 dengan pola serupa untuk melengkapi 90 soal. Struktur di atas sudah representatif.)

File 3: mahasiswa_log_jawaban.csv
Berikut sampel representatif yang mencerminkan 3 profil mahasiswa:
csvNIM,ID_Soal,Jawaban_Mahasiswa,Skor_Biner
2301001,Q001,A,1
2301001,Q002,B,1
2301001,Q003,A,1
2301001,Q004,A,1
2301001,Q005,A,1
2301001,Q010,A,1
2301001,Q011,B,1
2301001,Q028,A,1
2301001,Q034,A,1
2301001,Q037,A,1
2301001,Q040,A,1
2301001,Q049,A,1
2301002,Q001,A,1
2301002,Q002,B,1
2301002,Q003,B,0
2301002,Q004,A,1
2301002,Q005,B,0
2301002,Q010,A,1
2301002,Q011,A,0
2301002,Q028,B,0
2301002,Q034,A,1
2301002,Q037,B,0
2301002,Q040,A,1
2301002,Q049,B,0
2301003,Q001,B,0
2301003,Q002,C,0
2301003,Q003,C,0
2301003,Q004,B,0
2301003,Q005,C,0
2301003,Q010,C,0
2301003,Q011,C,0
2301003,Q028,C,0
2301003,Q034,B,0
2301003,Q037,C,0
2301003,Q040,C,0
2301003,Q049,C,0
Keterangan profil:

2301001 → Mahir: hampir semua jawaban benar, termasuk soal berbobot tinggi
2301002 → Cukup: benar di konsep dasar, salah di soal lanjutan/berbobot tinggi
2301003 → Remedial: mayoritas salah di semua kategori

Untuk dataset penuh (~40 × 60 soal = 2.400 baris), logika generasinya adalah:
pythonimport pandas as pd
import numpy as np

np.random.seed(42)

nim_list = [f"230100{i}" for i in range(1, 41)]
soal_ids = [f"Q{str(i).zfill(3)}" for i in range(1, 61)]
kunci = {f"Q{str(i).zfill(3)}": "A" for i in range(1, 61)}
kunci.update({"Q002": "B", "Q006": "B", "Q008": "C", ...})  # sesuaikan

profil = {nim: "mahir" if i < 10 else ("cukup" if i < 30 else "remedial")
          for i, nim in enumerate(nim_list)}

accuracy_map = {"mahir": 0.85, "cukup": 0.55, "remedial": 0.25}
opsi = ["A", "B", "C", "D"]

rows = []
for nim in nim_list:
    p_benar = accuracy_map[profil[nim]]
    for soal in soal_ids:
        benar = np.random.random() < p_benar
        jawaban = kunci[soal] if benar else np.random.choice(
            [o for o in opsi if o != kunci[soal]])
        skor = 1 if jawaban == kunci[soal] else 0
        rows.append({"NIM": nim, "ID_Soal": soal,
                     "Jawaban_Mahasiswa": jawaban, "Skor_Biner": skor})

df = pd.DataFrame(rows)
df.to_csv("mahasiswa_log_jawaban.csv", index=False)

BAGIAN 2: INSTRUKSI TEKNIS IMPLEMENTASI ALGORITMA
2.1 Bayesian Knowledge Tracing (BKT)
Parameter Inisialisasi
BKT adalah Hidden Markov Model (HMM) dengan 4 parameter per materi/submateri:
ParameterSimbolNilai Awal RekomendasiKeteranganPrior KnowledgeP(L₀)0.20Probabilitas mahasiswa sudah tahu sebelum belajarTransition (Learn)P(T)0.15Probabilitas mahasiswa belajar setelah satu latihanGuessP(G)0.20Probabilitas menjawab benar meski belum tahuSlipP(S)0.10Probabilitas menjawab salah meski sudah tahu

Catatan penting: P(G) dan P(S) harus < 0.5 untuk menjaga model agar masuk akal secara probabilistik. Nilai-nilai di atas mengikuti standar yang digunakan dalam sistem ITS (Intelligent Tutoring Systems).

Rumus Update BKT (Forward Algorithm)
Diberikan observasi baru q (1 = benar, 0 = salah), update P(Lₜ) dilakukan dalam dua langkah:
Langkah 1 — Update Posterior P(Lₜ | observasi)
P(Lt∣obs=1)=P(Lt−1)⋅(1−P(S))P(Lt−1)⋅(1−P(S))+(1−P(Lt−1))⋅P(G)P(L_t | \text{obs}=1) = \frac{P(L_{t-1}) \cdot (1 - P(S))}{P(L_{t-1}) \cdot (1 - P(S)) + (1 - P(L_{t-1})) \cdot P(G)}P(Lt​∣obs=1)=P(Lt−1​)⋅(1−P(S))+(1−P(Lt−1​))⋅P(G)P(Lt−1​)⋅(1−P(S))​
P(Lt∣obs=0)=P(Lt−1)⋅P(S)P(Lt−1)⋅P(S)+(1−P(Lt−1))⋅(1−P(G))P(L_t | \text{obs}=0) = \frac{P(L_{t-1}) \cdot P(S)}{P(L_{t-1}) \cdot P(S) + (1 - P(L_{t-1})) \cdot (1 - P(G))}P(Lt​∣obs=0)=P(Lt−1​)⋅P(S)+(1−P(Lt−1​))⋅(1−P(G))P(Lt−1​)⋅P(S)​
Langkah 2 — Update Transisi (setelah satu langkah)
P(Lt+1)=P(Lt∣obs)+(1−P(Lt∣obs))⋅P(T)P(L_{t+1}) = P(L_t | \text{obs}) + (1 - P(L_t | \text{obs})) \cdot P(T)P(Lt+1​)=P(Lt​∣obs)+(1−P(Lt​∣obs))⋅P(T)
Implementasi Python
python# app/bkt_engine.py

from dataclasses import dataclass, field
from typing import List, Dict

@dataclass
class BKTParams:
    """Parameter BKT per materi/submateri."""
    p_l0: float = 0.20   # Prior knowledge
    p_t:  float = 0.15   # Transition (learn rate)
    p_g:  float = 0.20   # Guess probability
    p_s:  float = 0.10   # Slip probability

@dataclass
class BKTState:
    """State BKT untuk satu mahasiswa pada satu materi."""
    materi: str
    p_ln: float          # P(Ln) saat ini
    n_obs: int = 0       # Jumlah observasi

class BKTEngine:
    """
    Engine BKT untuk melacak penguasaan materi mahasiswa.
    
    Setiap materi memiliki parameter BKT independen.
    State per mahasiswa disimpan dalam dictionary.
    """
    
    MASTERY_THRESHOLD = 0.75  # Ambang batas "sudah dikuasai"
    
    def __init__(self, params_per_materi: Dict[str, BKTParams] = None):
        """
        params_per_materi: dict {nama_materi: BKTParams}
        Jika None, pakai default params untuk semua materi.
        """
        self.params = params_per_materi or {}
        # {nim: {materi: BKTState}}
        self.student_states: Dict[str, Dict[str, BKTState]] = {}
    
    def _get_params(self, materi: str) -> BKTParams:
        return self.params.get(materi, BKTParams())
    
    def _get_state(self, nim: str, materi: str) -> BKTState:
        if nim not in self.student_states:
            self.student_states[nim] = {}
        if materi not in self.student_states[nim]:
            p = self._get_params(materi)
            self.student_states[nim][materi] = BKTState(
                materi=materi, p_ln=p.p_l0
            )
        return self.student_states[nim][materi]
    
    def update(self, nim: str, materi: str, is_correct: int) -> float:
        """
        Update P(Ln) berdasarkan satu observasi jawaban.
        
        Args:
            nim: NIM mahasiswa
            materi: Nama materi/submateri
            is_correct: 1 jika benar, 0 jika salah
        
        Returns:
            P(Ln) yang sudah diupdate
        """
        state = self._get_state(nim, materi)
        p = self._get_params(materi)
        
        p_ln = state.p_ln
        
        # Langkah 1: Update posterior berdasarkan observasi
        if is_correct == 1:
            numerator = p_ln * (1 - p.p_s)
            denominator = numerator + (1 - p_ln) * p.p_g
        else:
            numerator = p_ln * p.p_s
            denominator = numerator + (1 - p_ln) * (1 - p.p_g)
        
        p_ln_given_obs = numerator / denominator if denominator > 0 else p_ln
        
        # Langkah 2: Update transisi (probabilitas belajar setelah observasi)
        p_ln_next = p_ln_given_obs + (1 - p_ln_given_obs) * p.p_t
        
        # Simpan state yang baru
        state.p_ln = p_ln_next
        state.n_obs += 1
        
        return p_ln_next
    
    def process_log(self, log_df):
        """
        Proses seluruh log jawaban mahasiswa (DataFrame).
        Kolom yang dibutuhkan: NIM, Materi, Skor_Biner
        (join kuis_struktur untuk mendapatkan Materi)
        """
        for _, row in log_df.iterrows():
            self.update(row['NIM'], row['Materi'], row['Skor_Biner'])
    
    def get_mastery_vector(self, nim: str) -> Dict[str, float]:
        """
        Mengembalikan vektor penguasaan {materi: P(Ln)} untuk satu mahasiswa.
        Ini yang akan menjadi feature vector untuk K-Means.
        """
        if nim not in self.student_states:
            return {}
        return {
            materi: state.p_ln
            for materi, state in self.student_states[nim].items()
        }
    
    def get_learning_path(self, nim: str, all_materi: List[str]) -> List[Dict]:
        """
        Menghasilkan Learning Path personal berdasarkan P(Ln).
        Urutan: Materi dengan P(Ln) TERENDAH diprioritaskan.
        
        Returns:
            List of dict: [{'materi': ..., 'p_ln': ..., 'status': ..., 'prioritas': ...}]
        """
        mastery = self.get_mastery_vector(nim)
        result = []
        
        for materi in all_materi:
            p_ln = mastery.get(materi, self._get_params(materi).p_l0)
            
            if p_ln >= self.MASTERY_THRESHOLD:
                status = "Dikuasai ✓"
            elif p_ln >= 0.50:
                status = "Perlu Penguatan"
            else:
                status = "Perlu Remedial !"
            
            result.append({
                'materi': materi,
                'p_ln': round(p_ln, 4),
                'status': status
            })
        
        # Sort: belum dikuasai dulu, lalu urutkan dari P(Ln) terkecil
        result.sort(key=lambda x: (x['p_ln'] >= self.MASTERY_THRESHOLD, x['p_ln']))
        
        for i, item in enumerate(result):
            item['prioritas'] = i + 1
        
        return result

2.2 K-Means Clustering
Feature Engineering: Dari Log Jawaban ke Feature Vector
Setelah BKT dijalankan, setiap mahasiswa memiliki mastery vector berupa:
Xi=[P(Ln)M1,P(Ln)M2,P(Ln)M3,P(Ln)M4,P(Ln)M5,P(Ln)M6]X_i = [P(L_n)_{M1}, P(L_n)_{M2}, P(L_n)_{M3}, P(L_n)_{M4}, P(L_n)_{M5}, P(L_n)_{M6}]Xi​=[P(Ln​)M1​,P(Ln​)M2​,P(Ln​)M3​,P(Ln​)M4​,P(Ln​)M5​,P(Ln​)M6​]
Contoh untuk 6 materi SISOP:
Xi=[PL,ManProses,PL,PenjCPU,PL,ManMem,PL,VirtMem,PL,FileSys,PL,IO]X_i = [P_{L,\text{ManProses}}, P_{L,\text{PenjCPU}}, P_{L,\text{ManMem}}, P_{L,\text{VirtMem}}, P_{L,\text{FileSys}}, P_{L,\text{IO}}]Xi​=[PL,ManProses​,PL,PenjCPU​,PL,ManMem​,PL,VirtMem​,PL,FileSys​,PL,IO​]

Mengapa menggunakan P(Ln) dari BKT daripada raw % skor?

P(Ln) memperhitungkan efek Guess dan Slip, sehingga lebih robust. Mahasiswa yang menebak banyak jawaban benar tidak akan diidentifikasi sebagai "mahir" secara keliru.

Penentuan Nilai K Optimal
python# app/clustering_engine.py

import numpy as np
import pandas as pd
from sklearn.cluster import KMeans
from sklearn.preprocessing import StandardScaler
from sklearn.metrics import silhouette_score
import matplotlib.pyplot as plt

class ClusteringEngine:
    
    def __init__(self):
        self.scaler = StandardScaler()
        self.kmeans = None
        self.optimal_k = None
        self.feature_matrix = None
        self.nim_list = None
    
    def build_feature_matrix(self, bkt_engine, nim_list: list, materi_list: list) -> np.ndarray:
        """
        Membangun feature matrix X dari BKT mastery vectors.
        
        Returns:
            X: ndarray shape (n_mahasiswa, n_materi)
        """
        self.nim_list = nim_list
        rows = []
        for nim in nim_list:
            mastery = bkt_engine.get_mastery_vector(nim)
            row = [mastery.get(m, 0.20) for m in materi_list]
            rows.append(row)
        
        self.feature_matrix = np.array(rows)
        return self.feature_matrix
    
    def find_optimal_k(self, X: np.ndarray, k_range: range = range(2, 8)) -> int:
        """
        Menentukan K optimal menggunakan Elbow Method + Silhouette Score.
        
        Strategy: Gabungkan kedua metrik untuk keputusan yang lebih robust.
        - Elbow Method: Cari 'siku' pada kurva inertia (WCSS)
        - Silhouette Score: Pilih K dengan skor tertinggi (semakin tinggi = lebih baik)
        """
        X_scaled = self.scaler.fit_transform(X)
        
        inertias = []
        silhouette_scores = []
        
        for k in k_range:
            km = KMeans(n_clusters=k, random_state=42, n_init=10)
            labels = km.fit_predict(X_scaled)
            inertias.append(km.inertia_)
            if k > 1:
                s_score = silhouette_score(X_scaled, labels)
                silhouette_scores.append(s_score)
        
        # Hitung elbow menggunakan metode kneedle (jarak dari garis lurus)
        x_vals = np.array(list(k_range))
        inertia_vals = np.array(inertias)
        
        # Normalisasi untuk perbandingan
        x_norm = (x_vals - x_vals.min()) / (x_vals.max() - x_vals.min())
        y_norm = (inertia_vals - inertia_vals.min()) / (inertia_vals.max() - inertia_vals.min())
        
        # Jarak titik dari garis lurus (titik pertama ke titik terakhir)
        distances = np.abs(
            (y_norm[-1] - y_norm[0]) * x_norm - (x_norm[-1] - x_norm[0]) * y_norm +
            x_norm[-1] * y_norm[0] - y_norm[-1] * x_norm[0]
        ) / np.sqrt((y_norm[-1] - y_norm[0])**2 + (x_norm[-1] - x_norm[0])**2)
        
        elbow_k = list(k_range)[np.argmax(distances)]
        
        # Pilih K dengan silhouette tertinggi
        if silhouette_scores:
            sil_k = list(k_range)[1 + np.argmax(silhouette_scores)]
        else:
            sil_k = elbow_k
        
        # Konsensus: jika sama pakai itu, jika beda ambil yang lebih kecil (lebih konservatif)
        self.optimal_k = min(elbow_k, sil_k) if elbow_k != sil_k else elbow_k
        
        print(f"Elbow K: {elbow_k}, Silhouette K: {sil_k} → Optimal K: {self.optimal_k}")
        return self.optimal_k
    
    def fit(self, X: np.ndarray, k: int = None) -> np.ndarray:
        """
        Fit K-Means dan return label cluster tiap mahasiswa.
        """
        if k is None:
            k = self.optimal_k or self.find_optimal_k(X)
        
        X_scaled = self.scaler.transform(X)
        self.kmeans = KMeans(n_clusters=k, random_state=42, n_init=10)
        labels = self.kmeans.fit_predict(X_scaled)
        return labels
    
    def get_cluster_profile(self, X: np.ndarray, labels: np.ndarray, 
                            materi_list: list) -> pd.DataFrame:
        """
        Menghitung centroid tiap cluster untuk interpretasi profil.
        
        Returns:
            DataFrame: rata-rata P(Ln) per materi per cluster
        """
        df = pd.DataFrame(X, columns=materi_list)
        df['cluster'] = labels
        return df.groupby('cluster').mean().round(3)
Label Cluster (Interpretasi Semantik)
Setelah clustering selesai, centroid cluster di-rank berdasarkan rata-rata P(Ln) keseluruhan untuk memberikan label semantik:
pythondef label_clusters(cluster_profile_df: pd.DataFrame) -> Dict[int, str]:
    """
    Memberi label semantik pada cluster berdasarkan rata-rata kompetensi.
    """
    avg_mastery = cluster_profile_df.mean(axis=1).sort_values(ascending=False)
    labels_map = {}
    rank_labels = ["Mahir", "Cukup", "Perlu Bimbingan"]
    
    for rank, cluster_id in enumerate(avg_mastery.index):
        label = rank_labels[rank] if rank < len(rank_labels) else f"Cluster-{rank}"
        labels_map[cluster_id] = label
    
    return labels_map

2.3 Greedy Algorithm untuk Pembentukan Kelompok Heterogen
Formulasi Masalah
Input: Set mahasiswa S={s1,s2,...,sN}S = \{s_1, s_2, ..., s_N\}
S={s1​,s2​,...,sN​}, masing-masing dengan label cluster ci∈{0,1,...,K−1}c_i \in \{0, 1, ..., K-1\}
ci​∈{0,1,...,K−1} dan mastery vector XiX_i
Xi​.
Output: Partisi G={G1,G2,...,GM}G = \{G_1, G_2, ..., G_M\}
G={G1​,G2​,...,GM​} di mana M=⌊N/g⌋M = \lfloor N/g \rfloor
M=⌊N/g⌋ dan gg
g adalah target ukuran kelompok, sedemikian rupa sehingga setiap kelompok GjG_j
Gj​ bersifat heterogen (terdiri dari mahasiswa dari cluster berbeda).
Objective Function:
Heterogeneity(Gj)=∣{ci:si∈Gj}∣∣Gj∣\text{Heterogeneity}(G_j) = \frac{|\{c_i : s_i \in G_j\}|}{|G_j|}Heterogeneity(Gj​)=∣Gj​∣∣{ci​:si​∈Gj​}∣​
Diversity Score(Gj)=Var({P(Ln)avg,i:si∈Gj})\text{Diversity Score}(G_j) = \text{Var}(\{P(L_n)_{\text{avg},i} : s_i \in G_j\})Diversity Score(Gj​)=Var({P(Ln​)avg,i​:si​∈Gj​})
Semakin tinggi kedua nilai ini, semakin baik kelompok tersebut.
Implementasi Greedy
python# app/grouping_engine.py

import numpy as np
from typing import List, Dict, Tuple
from collections import defaultdict

class GroupingEngine:
    
    def __init__(self, group_size: int = 4):
        self.group_size = group_size
    
    def form_groups(self,
                    nim_list: List[str],
                    cluster_labels: np.ndarray,
                    feature_matrix: np.ndarray,
                    cluster_semantic: Dict[int, str],
                    materi_list: List[str]) -> List[Dict]:
        """
        Greedy algorithm untuk membentuk kelompok heterogen.
        
        Strategy:
        1. Sort mahasiswa berdasarkan cluster (urutan: Mahir, Cukup, Remedial)
        2. Gunakan Round-Robin Greedy: ambil satu mahasiswa dari tiap cluster
           secara bergiliran sampai kelompok penuh.
        3. Sisa mahasiswa (jika N tidak habis dibagi group_size) 
           didistribusikan ke kelompok yang paling heterogen.
        
        Returns:
            List of group dictionaries dengan informasi lengkap + reasoning
        """
        n = len(nim_list)
        
        # Kelompokkan mahasiswa per cluster
        cluster_buckets: Dict[int, List[int]] = defaultdict(list)
        for idx, nim in enumerate(nim_list):
            cluster_id = int(cluster_labels[idx])
            cluster_buckets[cluster_id].append(idx)
        
        # Sort cluster berdasarkan rank (Mahir dulu, lalu Cukup, lalu Remedial)
        avg_mastery_per_cluster = {}
        for c_id, indices in cluster_buckets.items():
            avg_mastery_per_cluster[c_id] = np.mean(feature_matrix[indices].mean(axis=1))
        
        sorted_clusters = sorted(
            cluster_buckets.keys(),
            key=lambda c: avg_mastery_per_cluster[c],
            reverse=True  # Mahir di depan
        )
        
        # Round-Robin Greedy Assignment
        groups_indices = []  # List of lists of student indices
        current_group = []
        
        # Iterator round-robin antar cluster
        cluster_pointers = {c: 0 for c in sorted_clusters}
        
        # Flatten dengan round-robin
        assigned = set()
        while len(assigned) < n:
            progress = False
            for c_id in sorted_clusters:
                bucket = cluster_buckets[c_id]
                ptr = cluster_pointers[c_id]
                
                # Temukan mahasiswa berikutnya yang belum di-assign
                while ptr < len(bucket) and bucket[ptr] in assigned:
                    ptr += 1
                cluster_pointers[c_id] = ptr
                
                if ptr < len(bucket):
                    idx = bucket[ptr]
                    current_group.append(idx)
                    assigned.add(idx)
                    cluster_pointers[c_id] = ptr + 1
                    progress = True
                    
                    if len(current_group) == self.group_size:
                        groups_indices.append(current_group)
                        current_group = []
            
            if not progress:
                break
        
        # Sisa mahasiswa → distribusikan ke kelompok terakhir
        if current_group:
            groups_indices.append(current_group)
        
        # Build output dengan reasoning
        groups_output = []
        for g_idx, group in enumerate(groups_indices):
            group_members = []
            for idx in group:
                nim = nim_list[idx]
                c_id = int(cluster_labels[idx])
                mastery_vec = feature_matrix[idx]
                avg_mastery = float(np.mean(mastery_vec))
                
                group_members.append({
                    'nim': nim,
                    'cluster_id': c_id,
                    'cluster_label': cluster_semantic.get(c_id, f"Cluster-{c_id}"),
                    'avg_mastery': round(avg_mastery, 3),
                    'mastery_vector': {
                        materi: round(float(mastery_vec[i]), 3)
                        for i, materi in enumerate(materi_list)
                    }
                })
            
            reason = self._generate_reasoning(group_members, materi_list)
            
            groups_output.append({
                'kelompok_id': g_idx + 1,
                'anggota': group_members,
                'heterogeneity_score': self._calc_heterogeneity(group_members),
                'reasoning': reason
            })
        
        return groups_output
    
    def _calc_heterogeneity(self, members: List[Dict]) -> float:
        """
        Heterogeneity = proporsi cluster unik dalam kelompok.
        Score = 1.0 berarti semua anggota dari cluster berbeda (ideal).
        """
        clusters = [m['cluster_id'] for m in members]
        unique_ratio = len(set(clusters)) / len(clusters)
        return round(unique_ratio, 3)
    
    def _generate_reasoning(self, members: List[Dict], materi_list: List[str]) -> str:
        """
        Menghasilkan string reasoning mengapa kelompok ini dibentuk.
        
        Logic:
        1. Identifikasi siapa "mentor" (mahir) dan siapa yang butuh bantuan
        2. Identifikasi keunggulan spesifik tiap anggota pada materi tertentu
        3. Format reasoning sebagai narasi yang readable
        """
        reasons = []
        
        # Sort by avg_mastery descending
        sorted_members = sorted(members, key=lambda m: m['avg_mastery'], reverse=True)
        
        # Identifikasi peran
        top = sorted_members[0]
        bottom = sorted_members[-1]
        
        reasons.append(
            f"Kelompok ini dirancang heterogen dari {len(set(m['cluster_id'] for m in members))} "
            f"level kompetensi berbeda."
        )
        
        reasons.append(
            f"{top['nim']} ({top['cluster_label']}, avg mastery={top['avg_mastery']:.1%}) "
            f"berperan sebagai peer-mentor untuk anggota lain."
        )
        
        # Identifikasi materi unggulan & kelemahan
        for m in sorted_members[1:]:
            strong_materi = max(
                materi_list, key=lambda mat: m['mastery_vector'].get(mat, 0)
            )
            weak_materi = min(
                materi_list, key=lambda mat: m['mastery_vector'].get(mat, 0)
            )
            
            # Cari siapa yang kuat di weak_materi tersebut
            helper = max(
                sorted_members,
                key=lambda x: x['mastery_vector'].get(weak_materi, 0)
                if x['nim'] != m['nim'] else -1
            )
            
            reasons.append(
                f"{m['nim']} ({m['cluster_label']}) unggul di '{strong_materi}' "
                f"(P(L)={m['mastery_vector'].get(strong_materi, 0):.1%}) "
                f"namun perlu penguatan di '{weak_materi}' "
                f"(P(L)={m['mastery_vector'].get(weak_materi, 0):.1%}), "
                f"di mana {helper['nim']} dapat membantu "
                f"(P(L)={helper['mastery_vector'].get(weak_materi, 0):.1%})."
            )
        
        return " ".join(reasons)
Contoh Output Reasoning yang Dihasilkan
Kelompok 1:
- Anggota: [2301001 (Mahir), 2301012 (Cukup), 2301035 (Cukup), 2301038 (Remedial)]
- Heterogeneity Score: 0.75
- Reasoning: "Kelompok ini dirancang heterogen dari 3 level kompetensi berbeda. 
  2301001 (Mahir, avg mastery=82.3%) berperan sebagai peer-mentor untuk anggota lain. 
  2301012 (Cukup) unggul di 'Manajemen Proses' (P(L)=71.2%) namun perlu penguatan di 
  'Virtual Memory' (P(L)=38.1%), di mana 2301001 dapat membantu (P(L)=88.4%). 
  2301038 (Perlu Bimbingan) unggul di 'Konsep I/O' (P(L)=41.0%) namun perlu penguatan 
  di 'Deadlock' (P(L)=12.3%), di mana 2301001 dapat membantu (P(L)=91.2%)."

Rangkuman Arsitektur Modul app/
app/
├── bkt_engine.py          # BKTParams, BKTState, BKTEngine
├── clustering_engine.py   # ClusteringEngine (feature matrix + K-Means)
├── grouping_engine.py     # GroupingEngine (Greedy + Reasoning)
├── data_loader.py         # Load & join CSV mahasiswa, kuis_struktur, log_jawaban
├── pipeline.py            # Orkestrasi: load → BKT → Cluster → Group → Output
└── dashboard.py           # Visualisasi (matplotlib/plotly) untuk output dosen
Pipeline Utama (pipeline.py)
python# Alur eksekusi lengkap
def run_pipeline(mahasiswa_csv, kuis_csv, log_csv, group_size=4):
    # 1. Load data
    df_mhs = pd.read_csv(mahasiswa_csv)
    df_kuis = pd.read_csv(kuis_csv)
    df_log = pd.read_csv(log_csv).merge(df_kuis[['ID_Soal','Materi']], on='ID_Soal')
    
    # 2. BKT: update penguasaan materi per mahasiswa
    bkt = BKTEngine()
    bkt.process_log(df_log)
    
    materi_list = df_kuis['Materi'].unique().tolist()
    nim_list = df_mhs['NIM'].astype(str).tolist()
    
    # 3. Clustering: bangun feature matrix → cari K optimal → fit
    ce = ClusteringEngine()
    X = ce.build_feature_matrix(bkt, nim_list, materi_list)
    optimal_k = ce.find_optimal_k(X)
    cluster_labels = ce.fit(X, k=optimal_k)
    cluster_profile = ce.get_cluster_profile(X, cluster_labels, materi_list)
    cluster_semantic = label_clusters(cluster_profile)
    
    # 4. Greedy Grouping
    ge = GroupingEngine(group_size=group_size)
    groups = ge.form_groups(nim_list, cluster_labels, X, cluster_semantic, materi_list)
    
    # 5. Learning Path per mahasiswa
    learning_paths = {
        nim: bkt.get_learning_path(nim, materi_list)
        for nim in nim_list
    }
    
    return {
        'groups': groups,
        'learning_paths': learning_paths,
        'cluster_profile': cluster_profile,
        'cluster_labels': dict(zip(nim_list, cluster_labels.tolist()))
    }

Semua kode di atas sudah dirancang agar modular, siap dikonversi ke modul Python di app/, dan mengikuti konvensi Python modern (dataclass, type hints, docstring). Langkah selanjutnya yang disarankan adalah:

Jalankan script generasi CSV untuk menghasilkan dataset dummy penuh
Unit test setiap engine secara terpisah sebelum menjalankan pipeline
Kalibrasi parameter BKT dengan data nyata menggunakan Expectation-Maximization (EM) jika tersedia data historis
You said: Untuk dataset penuh (~40 × 60 soal = 2.Untuk dataset penuh (~40 × 60 soal = 2.400 baris), logika generasinya adalah:
pythonimport pandas as pd
import numpy as np

np.random.seed(42)

nim_list = [f"230100{i}" for i in range(1, 41)]
soal_ids = [f"Q{str(i).zfill(3)}" for i in range(1, 61)]
kunci = {f"Q{str(i).zfill(3)}": "A" for i in range(1, 61)}
kunci.update({"Q002": "B", "Q006": "B", "Q008": "C", ...})  # sesuaikan

profil = {nim: "mahir" if i < 10 else ("cukup" if i < 30 else "remedial")
          for i, nim in enumerate(nim_list)}

accuracy_map = {"mahir": 0.85, "cukup": 0.55, "remedial": 0.25}
opsi = ["A", "B", "C", "D"]

rows = []
for nim in nim_list:
    p_benar = accuracy_map[profil[nim]]
    for soal in soal_ids:
        benar = np.random.random() < p_benar
        jawaban = kunci[soal] if benar else np.random.choice(
            [o for o in opsi if o != kunci[soal]])
        skor = 1 if jawaban == kunci[soal] else 0
        rows.append({"NIM": nim, "ID_Soal": soal,
                     "Jawaban_Mahasiswa": jawaban, "Skor_Biner": skor})

df = pd.DataFrame(rows)
df.to_csv("mahasiswa_log_jawaban.csv", index=False)bagian kode ini ditaruh di file apaShow more8:58 PMClaude responded: Kode generasi dataset dummy itu bukan bagian dari aplikasi utama — fungsinya hanya untuk sekali jalan menghasilkan file CSV.Kode generasi dataset dummy itu bukan bagian dari aplikasi utama — fungsinya hanya untuk sekali jalan menghasilkan file CSV. Jadi penempatannya yang tepat adalah:
project-AI-reasoning/
├── app/                          # Source code aplikasi utama
├── dataset/                      # Folder output CSV (di Google Drive)
├── docs/
│   └── week1/
├── scripts/                      # ← TARUH DI SINI (buat folder baru)
│   └── generate_dummy_data.py    # ← file ini
├── AI_05.ipynb
├── requirements.txt
└── README.md
Alasannya:

Bukan logika aplikasi — tidak akan dipanggil saat sistem berjalan. File app/ berisi engine yang dipakai runtime (BKT, Clustering, Grouping).
Bukan notebook eksperimen — lebih rapi sebagai .py terpisah daripada dicampur di AI_05.ipynb, supaya bisa dijalankan ulang via CLI kapan saja: python scripts/generate_dummy_data.py
Konvensi umum proyek ML/DS — folder scripts/ atau tools/ lazim dipakai untuk utilitas satu kali jalan seperti: data generation, data migration, seeding database.

Selain itu, lengkapi kodenya dengan path output yang eksplisit dan dict kunci yang sudah terisi penuh sebelum dijalankan:
python# scripts/generate_dummy_data.py

import pandas as pd
import numpy as np
from pathlib import Path

OUTPUT_DIR = Path(__file__).parent.parent / "dataset"
OUTPUT_DIR.mkdir(exist_ok=True)

# Kunci jawaban lengkap sesuai kuis_struktur.csv
KUNCI = {
    "Q001": "A", "Q002": "B", "Q003": "A", "Q004": "A", "Q005": "A",
    "Q006": "B", "Q007": "D", "Q008": "C", "Q009": "A", "Q010": "A",
    # ... dst lengkap 60 soal
}

if __name__ == "__main__":
    # ... logika generate ...
    df.to_csv(OUTPUT_DIR / "mahasiswa_log_jawaban.csv", index=False)
    print(f"Dataset berhasil dibuat: {len(df)} baris")
Kalau mau, saya bisa langsung buatkan file generate_dummy_data.py yang lengkap dan siap jalan — termasuk generate ketiga CSV sekaligus (mahasiswa.csv, kuis_struktur.csv, mahasiswa_log_jawaban.csv) dalam satu eksekusi.